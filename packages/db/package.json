{
  "name": "@agent-flow/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "db:generate": "cross-env DATABASE_URL=file:./dev.db prisma generate --schema prisma/schema.prisma",
    "db:push": "cross-env DATABASE_URL=file:./dev.db prisma db push --schema prisma/schema.prisma --skip-generate",
    "db:validate": "cross-env DATABASE_URL=file:./dev.db prisma validate --schema prisma/schema.prisma",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "prisma": "^6.19.0",
    "vitest": "^3.2.0"
  }
}
